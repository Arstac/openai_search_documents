# Archivo de variables de entorno
# Copia este archivo como .env y configura tus valores

# OpenAI Configuration
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_MODEL=gpt-4o

# Application Configuration
APP_NAME=Q&A sobre archivos con OpenAI
APP_DESCRIPTION=Sube un archivo y pregúntale al modelo sobre su contenido
APP_VERSION=1.0.0
DEBUG=false

# CORS Configuration (opcional, los valores por defecto permiten todo)
# ALLOWED_ORIGINS=["http://localhost:3000", "https://tu-frontend.com"]
# ALLOWED_METHODS=["GET", "POST", "PUT", "DELETE"]
# ALLOWED_HEADERS=["*"]
# ALLOW_CREDENTIALS=true

# File Upload Configuration (opcional)
# MAX_FILE_SIZE=10485760  # 10MB en bytes
# ALLOWED_FILE_TYPES=["text/plain", "application/pdf", "application/json"]
